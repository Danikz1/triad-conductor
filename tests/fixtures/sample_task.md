# Sample Task

Build a simple "hello world" CLI application in Python.

## Requirements
- Create a `hello.py` file that prints "Hello, World!" when run
- Add a `--name` argument to greet a specific person
- Include basic unit tests

## Constraints
- Python 3.11+
- No external dependencies
- Must pass all tests
